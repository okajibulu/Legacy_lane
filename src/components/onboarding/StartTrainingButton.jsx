import React from 'react';

const StartTrainingButton = ({ onStart }) => (
  <button className="start-training" onClick={onStart}>
    Start Training
  </button>
);

export default StartTrainingButton;

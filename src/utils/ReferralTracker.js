export const logReferral = ({ referralCode, fullName }) => {
  // Simulate referral logging
  console.log(`Referral logged for ${fullName} via code: ${referralCode}`);
  // TODO: Send to backend or trigger badge logic
};
